La BD está configurada para user = root y password = 1234

1) Agregar los jars que faltan en WEB-CONTENT/WEB-INF/lib

2) Correr el script schema.sql que crea las tablas y datos de ejemplo

3) Levantar el server

4) Correr los test de postman LPB.postman_collection.json

Aclaración:
Los errores en el uso de la api (ej: pasar un id inválido) se identifican por el codigo del response http y el header LPB-Message que tiene el mensaje detallado
